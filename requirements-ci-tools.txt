# Outils de certification CI, versions exactes.
# Les dépendances runtime restent gérées par pyproject.toml ; la release portable
# garde son wheelhouse exact + hashes dans requirements-portable.txt.
coverage==7.15.2
pip-audit==2.10.1
