# Realtime Monitor

Read-only WebSocket planning primitives. The monitor is disabled by default and
requires bounded duration or dry-run mode. User-specific subscriptions are
limited by configuration and no execution path exists.
