# Explorer Observer

Experimental, read-only and disabled by default. It supports manual imports and
fixtures for events visible around the Hyperliquid explorer. Network probing is
not enabled until an endpoint is verified and explicitly allowed.
