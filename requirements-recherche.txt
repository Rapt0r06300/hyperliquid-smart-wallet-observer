# Dépendances OPTIONNELLES du laboratoire de recherche continu (Flo, FX-3).
# Le labo FONCTIONNE sans elles : grid / random / QMC (Halton) sont purs Python et toujours disponibles.
# Ces paquets ACTIVENT les optimiseurs avancés d'Optuna (samplers + pruners).
#
# Reproductibilité H—T-50 : versions directes exactes, alignées sur
# requirements-portable.txt. Ceci stabilise la résolution directe sans prétendre
# à un hash-lock transitif complet ; le lock portable reste l'autorité hashée.
# Toute mise à jour doit être volontaire, réauditée et propagée aux deux surfaces.
#
# Installation : python -m pip install -r requirements-recherche.txt
#
# SÉCURITÉ : 0 ordre réel · 0 argent réel · 0 clé privée · 0 signature · 0 dépôt/retrait. Paper-only, read-only.

optuna==4.9.0
cmaes==0.13.0
scipy==1.18.0
numpy==2.5.1
pyarrow==25.0.0
lz4==4.4.5
