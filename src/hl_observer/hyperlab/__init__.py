"""[hyperlab] Sous-systeme operationnel HyperSmart : data plane reel (medaillon Bronze/Silver/Gold +
catalogue Data Mesh), gate LIVE_READY (OFFLINE_READY != LIVE_READY). Import du data plane pour cablage
reel des composants (medallion_store, data_mesh_catalog, live_ready)."""
from . import data_plane  # noqa: F401
