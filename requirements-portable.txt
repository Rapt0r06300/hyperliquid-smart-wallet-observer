# HyperSmart portable Windows runtime.
#
# This file is intentionally broader than requirements.txt: the single launcher
# starts the UI, collectors, the continuous research lab and the local QA tools.
# Everything below remains local/read-only/paper-only.

# Core application (mirrors pyproject.toml).
fastapi>=0.115,<1
httpx>=0.27,<1
pydantic>=2.5,<3
PyYAML>=6,<7
SQLAlchemy>=2.0,<3
typer>=0.12,<1
uvicorn>=0.30,<1
websocket-client>=1.8,<2
websockets>=10,<11

# Runtime supervision and terminal dashboards.
psutil>=5.9,<8
rich>=13,<15
requests==2.32.5
aiohttp>=3.9,<4
lz4>=4,<5

# Research and replay stack used by the continuous laboratory.
numpy>=1.24,<3
scipy>=1.11,<2
pandas>=2,<3
optuna>=3.6,<5
cmaes>=0.10,<1

# Local QA commands exposed by the unified launcher.
pytest>=8,<9
pytest-timeout>=2,<3
pytest-xdist>=3,<4
coverage>=7,<8
hypothesis>=6,<7
ruff>=0.5,<1
mypy>=1.5,<2
