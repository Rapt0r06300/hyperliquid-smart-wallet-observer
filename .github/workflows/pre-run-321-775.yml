name: pre-run-321-775-honest-completion

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    name: Disabled - use ci.yml instead
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pre-run 775 job disabled. Core CI runs in ci.yml"
