# HyperSmart Data Pipeline Map

```text
read-only observation
  -> wallet discovery
  -> local fills/events
  -> lifecycle reconstruction
  -> scoring
  -> smart ranking V2
  -> pattern detection
  -> backtest replay
  -> local paper simulation
  -> read-only dashboard
  -> audit/release reports
```

Every transition is research-only.
