# CI diagnostic probe — GPT-5.6 Sol

Temporary no-code probe for the current `main` state. This branch exists only to expose pull-request GitHub Actions logs through the connected GitHub tooling. It must not be merged.
