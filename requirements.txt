# Dépendances du projet (IDEA-76 — environnement reproductible).
# Le TOOLKIT QUANT (src/hl_observer/backtesting/*) est en Python PUR : ZÉRO dépendance.
# Les dépendances ci-dessous ne servent qu'au runtime de collecte / aux tests.
#
# Pour figer les versions EXACTES sur ta machine :
#     pip freeze > requirements.lock.txt

# --- Runtime (inchangé, pins existants conservés) ---
pytest>=8,<9
httpx>=0.27,<1
websockets>=10,<11

# --- Qualité (optionnel — configs prêtes : ruff.toml, mypy.ini) ---
# Non installés par défaut ; à ajouter quand tu veux lancer le lint/typage :
#   pip install ruff mypy
# ruff>=0.1
# mypy>=1.5
